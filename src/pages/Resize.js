import React, { useState, useEffect } from "react"
import ResizeError from "../components/ResizeError/ResizeError"
import Layout from "../components/Layout/Layout"

const Resize = () => {
  return (
    <Layout>
      <ResizeError></ResizeError>
    </Layout>
  )
}

export default Resize
